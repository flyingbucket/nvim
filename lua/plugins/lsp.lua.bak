return {
  "neovim/nvim-lspconfig",
  opts = {
    servers = {
      clangd = {
        cmd = { 
          "clangd", 
          "--background-index" ,
          "--cuda-path=/usr/local/cuda-12.8",
        },
        filetypes = { "c", "cpp", "cuda" },
        init_options = {
          clangdFileStatus = true,
        },
      },
    },
  },
}
